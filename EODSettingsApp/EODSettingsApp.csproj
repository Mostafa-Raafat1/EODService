<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <AssemblyName>EODServiceManager</AssemblyName>
    <RootNamespace>EODSettingsApp</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
    <ErrorOnDuplicatePublishOutputFiles>false</ErrorOnDuplicatePublishOutputFiles>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\EODService\EODService.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Pin these to the same versions used by EODService to avoid assembly conflicts -->
    <PackageReference Include="Microsoft.EntityFrameworkCore"          Version="10.0.10" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Configuration"     Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console"   Version="10.0.10" />
    <PackageReference Include="TaskScheduler"                          Version="2.11.0" />
  </ItemGroup>


  <ItemGroup>
    <None Update="PathesConfig.json">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <!-- After each build, copy EODServiceManager.exe next to EODService.exe -->
  <!-- Uses MSBuildThisFileDirectory (always defined) instead of SolutionDir (undefined in single-project builds) -->
  <Target Name="CopyToEODServiceOutput" AfterTargets="Build">
    <PropertyGroup>
      <EODServiceOutputDir>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\EODService\bin\$(Configuration)\net10.0\'))</EODServiceOutputDir>
    </PropertyGroup>
    <ItemGroup>
      <!-- Only copy the main EXE and its direct support files — not sub-language folders -->
      <FilesToCopy Include="$(OutputPath)EODServiceManager.exe" />
      <FilesToCopy Include="$(OutputPath)EODServiceManager.dll" />
      <FilesToCopy Include="$(OutputPath)EODServiceManager.runtimeconfig.json" />
      <FilesToCopy Include="$(OutputPath)EODServiceManager.deps.json" />
      <FilesToCopy Include="$(OutputPath)PathesConfig.json" />
    </ItemGroup>
    <Copy
      SourceFiles="@(FilesToCopy)"
      DestinationFolder="$(EODServiceOutputDir)"
      SkipUnchangedFiles="true"
      Condition="Exists('$(EODServiceOutputDir)')" />
  </Target>

</Project>
